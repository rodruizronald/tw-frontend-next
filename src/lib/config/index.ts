/**
 * Configuration Module Exports
 *
 * Import the appropriate config based on your context:
 * - Client Components: import { clientConfig } from '@/lib/config/client'
 * - Server Components: import { serverConfig } from '@/lib/config/server'
 */

// Re-export types
export type { ClientConfig } from './client'
export type { ServerConfig } from './server'
